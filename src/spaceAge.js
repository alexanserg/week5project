export class spaceAge {
  constructor(birthDate, todaysDate, earthAge, expect, merYears, venYears, marYears, jupYears) {
    this.birthdate = inputtedBirthDate;
    this.todaysDate = new Date().toISOString().slice(0, 10)
    this.earthAge = presentDate - birthDate;
    this.expect = averageLife-earthAge;
  }
}
