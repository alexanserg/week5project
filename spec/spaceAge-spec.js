import { spaceAge } from "../src/spaceAge.js"

describe("spaceAge") function () {
  it('should test that age in earth years are returned correctly', function() {
    expect(earthAge(date)).toEqual(earthAge);
  }) {
    it('should test that life expectancy in earth years are returned correctly', function() {
      expect(earthExpectancy(date)).toEqual(earthExpectancy);
     {
    it('should test that age in mercury years are returned correctly', function() {
      expect(mercuryAge(date)).toEqual(mercuryAge);
    }) {
      it('should test that life expectancy in earth years are returned correctly', function() {
        expect(mercuryExpectancy(date)).toEqual(mercuryExpectancy);
      })

}
